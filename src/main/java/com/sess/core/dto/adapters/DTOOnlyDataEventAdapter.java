package com.sess.core.dto.adapters;

import com.sess.core.dto.DTOOnlyDataEvent;
import com.sess.core.events.Event;

public interface DTOOnlyDataEventAdapter extends DTOAdapter<DTOOnlyDataEvent, Event> {
}
